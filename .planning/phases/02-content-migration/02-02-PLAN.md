---
phase: 02-content-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pwa/src/lib/components/Article.svelte
  - pwa/src/lib/components/Admonition.svelte
  - pwa/src/lib/components/SourceBadge.svelte
  - pwa/src/lib/components/SourceSheet.svelte
  - pwa/src/lib/components/TableWrapper.svelte
  - pwa/src/lib/components/StarTable.svelte
  - pwa/src/lib/components/DiagramViewer.svelte
  - pwa/src/lib/components/ReadingProgress.svelte
  - pwa/src/lib/components/ChapterHeader.svelte
  - pwa/src/lib/components/ChapterSources.svelte
  - pwa/src/lib/styles/tokens.css
  - pwa/src/lib/styles/article.css
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06

must_haves:
  truths:
    - "Article component renders markdown content with proper typography hierarchy"
    - "Admonitions render with type-specific colors and Lucide icons"
    - "Source badges appear as colored chips at end of paragraphs"
    - "Source sheet opens as bottom sheet on tap showing full citation detail"
    - "Tables scroll horizontally with fade shadow indicator"
    - "Star tables have elevated card treatment with accent border"
    - "Diagrams render with fullscreen expand for complex types"
    - "Reading progress bar tracks scroll position"
  artifacts:
    - path: "pwa/src/lib/components/Article.svelte"
      provides: "Main article renderer for chapter content"
      contains: "vsa-article"
    - path: "pwa/src/lib/components/Admonition.svelte"
      provides: "Styled admonition block with icon and type-specific color"
      contains: "vsa-admonition"
    - path: "pwa/src/lib/components/SourceBadge.svelte"
      provides: "Inline source citation chip with tap handler"
      contains: "vsa-source-badge"
    - path: "pwa/src/lib/components/SourceSheet.svelte"
      provides: "Bottom sheet modal showing full source detail"
      contains: "vsa-source-sheet"
    - path: "pwa/src/lib/components/TableWrapper.svelte"
      provides: "Horizontal scroll container with fade shadow"
      contains: "vsa-table-wrapper"
    - path: "pwa/src/lib/components/DiagramViewer.svelte"
      provides: "SVG container with fullscreen expand for complex diagrams"
      contains: "vsa-diagram"
    - path: "pwa/src/lib/components/ReadingProgress.svelte"
      provides: "Fixed progress bar at top of viewport"
      contains: "vsa-reading-progress"
    - path: "pwa/src/lib/styles/tokens.css"
      provides: "Extended design tokens for source badges and admonitions"
      contains: "--vsa-source-multilateral"
  key_links:
    - from: "pwa/src/lib/components/SourceBadge.svelte"
      to: "pwa/src/lib/components/SourceSheet.svelte"
      via: "click handler opens sheet"
      pattern: "SourceSheet"
    - from: "pwa/src/lib/components/ReadingProgress.svelte"
      to: "pwa/src/lib/stores/reading-progress.js"
      via: "store subscription"
      pattern: "readingProgress"
---

<objective>
Build all content rendering Svelte components: Article (main renderer), Admonition (styled blocks), SourceBadge/SourceSheet (citation system), TableWrapper/StarTable (scrollable tables), DiagramViewer (SVG with fullscreen), ReadingProgress (scroll bar), ChapterHeader, and ChapterSources. Add the new design tokens (source badge colors, admonition colors) to tokens.css. Create article.css for typography styles.

Purpose: These components are what the user actually sees. Without them, the mdsvex pipeline produces unstyled HTML. These components transform raw markdown into the book reading experience described in the UI-SPEC.
Output: 10 Svelte components + extended tokens.css + article.css following the UI-SPEC design contract exactly.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-migration/02-CONTEXT.md
@.planning/phases/02-content-migration/02-UI-SPEC.md
@.planning/phases/02-content-migration/02-RESEARCH.md

<interfaces>
From pwa/src/lib/styles/tokens.css (will be extended):
```css
:root {
  --vsa-bg-primary: #09090B;
  --vsa-bg-secondary: #18181B;
  --vsa-bg-card: #1C1C1E;
  --vsa-text-primary: #FAFAFA;
  --vsa-text-secondary: #A1A1AA;
  --vsa-accent: #10B981;
  --vsa-accent-hover: #34D399;
  --vsa-accent-subtle: rgba(16, 185, 129, 0.12);
  --vsa-border: rgba(255, 255, 255, 0.06);
  --vsa-destructive: #EF4444;
  --vsa-shadow-card: 0 2px 8px rgba(0, 0, 0, 0.3);
  --vsa-shadow-elevated: 0 4px 16px rgba(0, 0, 0, 0.4);
  /* spacing, radius, typography tokens also defined */
}
```

From pwa/src/lib/content/sources.js (created in Plan 01):
```javascript
export const SOURCE_TYPES = { multilateral, energy, media, academic, projection };
export function classifySource(orgName): string;
```

From pwa/src/lib/stores/reading-progress.js (created in Plan 01):
```javascript
export const readingProgress: Writable<Record<string, { scrollPercent, lastVisited, lastScrollY }>>;
export const lastReadChapter: Readable<[string, object] | null>;
export function updateChapterProgress(chapterSlug, scrollPercent, scrollY): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 2 design tokens and create article typography styles</name>
  <files>pwa/src/lib/styles/tokens.css, pwa/src/lib/styles/article.css</files>
  <read_first>
    - pwa/src/lib/styles/tokens.css (current tokens to extend)
    - .planning/phases/02-content-migration/02-UI-SPEC.md (color section, typography section, spacing section)
  </read_first>
  <action>
**1. Extend `pwa/src/lib/styles/tokens.css`** — Add new tokens AFTER existing ones, inside the `:root` block for dark mode and `:root.light` block for light mode. Do NOT remove or modify any existing tokens.

Add to `:root` (dark mode):
```css
  /* === Phase 2: Source Badge Colors (Dark) === */
  --vsa-source-multilateral: #3B82F6;
  --vsa-source-multilateral-bg: rgba(59, 130, 246, 0.12);
  --vsa-source-energy: #10B981;
  --vsa-source-energy-bg: rgba(16, 185, 129, 0.12);
  --vsa-source-media: #A1A1AA;
  --vsa-source-media-bg: rgba(161, 161, 170, 0.12);
  --vsa-source-academic: #A78BFA;
  --vsa-source-academic-bg: rgba(167, 139, 250, 0.12);
  --vsa-source-projection: #10B981;
  --vsa-source-projection-bg: rgba(16, 185, 129, 0.12);

  /* === Phase 2: Admonition Colors (Dark) === */
  --vsa-info: #3B82F6;
  --vsa-info-bg: rgba(59, 130, 246, 0.08);
  --vsa-warning: #F59E0B;
  --vsa-warning-bg: rgba(245, 158, 11, 0.08);
  --vsa-danger-bg: rgba(239, 68, 68, 0.08);
```

Add to `:root.light`:
```css
  /* === Phase 2: Source Badge Colors (Light) === */
  --vsa-source-multilateral: #2563EB;
  --vsa-source-multilateral-bg: rgba(37, 99, 235, 0.08);
  --vsa-source-energy: #059669;
  --vsa-source-energy-bg: rgba(5, 150, 105, 0.08);
  --vsa-source-media: #71717A;
  --vsa-source-media-bg: rgba(113, 113, 122, 0.08);
  --vsa-source-academic: #7C3AED;
  --vsa-source-academic-bg: rgba(124, 58, 237, 0.08);
  --vsa-source-projection: #059669;
  --vsa-source-projection-bg: rgba(5, 150, 105, 0.08);

  /* === Phase 2: Admonition Colors (Light) === */
  --vsa-info: #2563EB;
  --vsa-info-bg: rgba(37, 99, 235, 0.06);
  --vsa-warning: #D97706;
  --vsa-warning-bg: rgba(217, 119, 6, 0.06);
  --vsa-danger-bg: rgba(220, 38, 38, 0.06);
```

Note: `--vsa-accent` (tip) and `--vsa-destructive` (danger) already exist and are reused per UI-SPEC.

**2. Create `pwa/src/lib/styles/article.css`** — Article typography mapping per UI-SPEC Typography section:

```css
/* Venezuela S.A. PWA — Article Typography
   Maps markdown heading levels to design tokens per UI-SPEC.
   Applied to .vsa-article container. */

.vsa-article {
  max-width: 680px;
  margin: 0 auto;
  padding: var(--vsa-space-3xl) var(--vsa-space-md) var(--vsa-space-3xl);
  color: var(--vsa-text-primary);
  font-family: var(--vsa-font-family);
  font-size: var(--vsa-font-size-base);
  line-height: 1.5;
}

/* Heading hierarchy */
.vsa-article h1 {
  font-size: var(--vsa-font-size-2xl);
  font-weight: 600;
  line-height: 1.2;
  margin: 0 0 var(--vsa-space-lg);
}

.vsa-article h2 {
  font-size: var(--vsa-font-size-lg);
  font-weight: 600;
  line-height: 1.3;
  margin: var(--vsa-space-2xl) 0 var(--vsa-space-md);
}

.vsa-article h3 {
  font-size: var(--vsa-font-size-lg);
  font-weight: 600;
  line-height: 1.3;
  margin: var(--vsa-space-xl) 0 var(--vsa-space-sm);
}

.vsa-article h4 {
  font-size: var(--vsa-font-size-base);
  font-weight: 600;
  line-height: 1.5;
  margin: var(--vsa-space-lg) 0 var(--vsa-space-sm);
}

/* Body elements */
.vsa-article p {
  margin: 0 0 var(--vsa-space-md);
}

.vsa-article ul, .vsa-article ol {
  margin: 0 0 var(--vsa-space-md);
  padding-left: var(--vsa-space-lg);
}

.vsa-article li {
  margin: 0 0 var(--vsa-space-sm);
}

.vsa-article blockquote {
  margin: var(--vsa-space-md) 0;
  padding: var(--vsa-space-md);
  border-left: 3px solid var(--vsa-border);
  color: var(--vsa-text-secondary);
}

/* Inline code */
.vsa-article code {
  font-size: 0.875em;
  background: var(--vsa-bg-secondary);
  padding: 2px 6px;
  border-radius: 4px;
}

/* Code blocks */
.vsa-article pre {
  margin: var(--vsa-space-md) 0;
  padding: var(--vsa-space-md);
  background: var(--vsa-bg-secondary);
  border-radius: var(--vsa-radius-sm);
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
}

.vsa-article pre code {
  background: none;
  padding: 0;
  font-size: 0.875em;
}

/* Links */
.vsa-article a {
  color: var(--vsa-accent);
  text-decoration: none;
}

.vsa-article a:hover {
  text-decoration: underline;
}

/* Strong and emphasis */
.vsa-article strong {
  font-weight: 600;
  color: var(--vsa-text-primary);
}

/* Images */
.vsa-article img {
  max-width: 100%;
  height: auto;
  border-radius: var(--vsa-radius-sm);
}

/* Horizontal rule */
.vsa-article hr {
  border: none;
  border-top: 1px solid var(--vsa-border);
  margin: var(--vsa-space-2xl) 0;
}

/* Section spacing — between docs within continuous scroll */
.vsa-article__section {
  margin-bottom: var(--vsa-space-2xl);
}

.vsa-article__section + .vsa-article__section {
  padding-top: var(--vsa-space-2xl);
  border-top: 1px solid var(--vsa-border);
}

/* Responsive: < 360px */
@media (max-width: 359px) {
  .vsa-article {
    padding-left: 12px;
    padding-right: 12px;
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/jw.castillo/Documents/personales/venezuela-sa/pwa && grep -c "vsa-source-multilateral" src/lib/styles/tokens.css && grep -c "vsa-info" src/lib/styles/tokens.css && test -f src/lib/styles/article.css && grep -c "vsa-article" src/lib/styles/article.css && echo "All token and article checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - tokens.css contains `--vsa-source-multilateral: #3B82F6` in `:root` block
    - tokens.css contains `--vsa-source-multilateral: #2563EB` in `:root.light` block
    - tokens.css contains `--vsa-source-energy`, `--vsa-source-media`, `--vsa-source-academic`, `--vsa-source-projection` with both dark and light values
    - tokens.css contains `--vsa-info: #3B82F6`, `--vsa-warning: #F59E0B`, `--vsa-danger-bg`
    - All existing tokens (--vsa-bg-primary, --vsa-accent, etc.) are unchanged
    - article.css contains `.vsa-article` with `max-width: 680px`
    - article.css contains heading styles h1 through h4 with correct font sizes
    - article.css contains `.vsa-article__section` for doc separation
    - article.css has responsive rule for < 360px
  </acceptance_criteria>
  <done>Design tokens extended with all 10 source badge color tokens (5 types x dark/light) and 6 admonition tokens. Article typography CSS maps markdown heading levels to design system. All per UI-SPEC color and typography specifications.</done>
</task>

<task type="auto">
  <name>Task 2: Create Admonition, SourceBadge, SourceSheet, TableWrapper, StarTable, and DiagramViewer components</name>
  <files>pwa/src/lib/components/Admonition.svelte, pwa/src/lib/components/SourceBadge.svelte, pwa/src/lib/components/SourceSheet.svelte, pwa/src/lib/components/TableWrapper.svelte, pwa/src/lib/components/StarTable.svelte, pwa/src/lib/components/DiagramViewer.svelte</files>
  <read_first>
    - pwa/src/lib/components/ComingSoon.svelte (existing Svelte 5 component pattern)
    - pwa/src/lib/components/Header.svelte (existing BEM pattern)
    - pwa/src/lib/styles/tokens.css (design tokens including new Phase 2 tokens)
    - .planning/phases/02-content-migration/02-UI-SPEC.md (component inventory section — Admonition, SourceBadge, SourceSheet, TableWrapper, StarTable, DiagramViewer specs)
  </read_first>
  <action>
**1. Create `pwa/src/lib/components/Admonition.svelte`:**
- Props: `type` ('danger' | 'info' | 'tip' | 'caution' | 'warning'), `title` (string)
- BEM class: `vsa-admonition` with modifier `vsa-admonition--{type}`
- Structure:
  ```html
  <div class="vsa-admonition vsa-admonition--{type}" role="note" aria-label="{type}: {title}">
    <div class="vsa-admonition__header">
      <Icon size={16} /> <!-- AlertTriangle for danger/caution/warning, Info for info, Lightbulb for tip -->
      <span class="vsa-admonition__title">{title}</span>
    </div>
    <div class="vsa-admonition__body">
      {@render children()}
    </div>
  </div>
  ```
- Import icons from `@lucide/svelte`: `AlertTriangle`, `Info`, `Lightbulb`
- Use Svelte 5 snippets: `let { type, title, children } = $props();`
- Styles (scoped `<style>`):
  - Left border: 3px solid, color by type (danger/caution: `var(--vsa-destructive)`, info: `var(--vsa-info)`, tip: `var(--vsa-accent)`, warning: `var(--vsa-warning)`)
  - Background by type: danger: `var(--vsa-danger-bg)`, info: `var(--vsa-info-bg)`, tip: `var(--vsa-accent-subtle)`, warning: `var(--vsa-warning-bg)`, caution: `var(--vsa-warning-bg)`
  - Border-radius: `var(--vsa-radius-sm)` (8px)
  - Padding: `var(--vsa-space-md)` (16px)
  - Header: flex row, gap `var(--vsa-space-xs)` (4px), title is 16px semibold
  - Body: 16px regular

**2. Create `pwa/src/lib/components/SourceBadge.svelte`:**
- Props: `sources` (array of `{ organization, type, date, url, methodology }`)
- BEM class: `vsa-source-badge`
- Structure: inline `<span>` pill chip
  ```html
  <span
    class="vsa-source-badge vsa-source-badge--{primaryType}"
    role="button"
    tabindex="0"
    aria-label="Fuente: {orgNames}. Toca para ver detalles."
    onclick={openSheet}
    onkeydown={handleKeydown}
  >
    {orgNames}
  </span>
  ```
- `primaryType` = type of first source (determines color)
- `orgNames` = sources.map(s => s.organization).join(', ')
- `openSheet` dispatches a custom event or directly opens SourceSheet
- Use Svelte 5: `let { sources } = $props();` and `let sheetOpen = $state(false);`
- Styles:
  - Pill shape: `border-radius: var(--vsa-radius-full)` (9999px)
  - Padding: 2px 8px
  - Font: 12px caption (`var(--vsa-font-size-xs)`)
  - Color/background: CSS classes per type mapping to tokens:
    - `.vsa-source-badge--multilateral { color: var(--vsa-source-multilateral); background: var(--vsa-source-multilateral-bg); }`
    - `.vsa-source-badge--energy { color: var(--vsa-source-energy); background: var(--vsa-source-energy-bg); }`
    - `.vsa-source-badge--media { color: var(--vsa-source-media); background: var(--vsa-source-media-bg); }`
    - `.vsa-source-badge--academic { color: var(--vsa-source-academic); background: var(--vsa-source-academic-bg); }`
    - `.vsa-source-badge--projection { color: var(--vsa-source-projection); background: var(--vsa-source-projection-bg); }`
  - Min-height: 44px via padding to ensure touch target
- Include SourceSheet component inline, controlled by `sheetOpen` state

**3. Create `pwa/src/lib/components/SourceSheet.svelte`:**
- Props: `sources` (array), `open` (boolean), `onclose` (function)
- BEM class: `vsa-source-sheet`
- Uses native `<dialog>` element for modal behavior
- Structure:
  ```html
  <dialog class="vsa-source-sheet" bind:this={dialogEl}>
    <div class="vsa-source-sheet__handle"></div>
    <button class="vsa-source-sheet__close" aria-label="Cerrar" onclick={onclose}><X size={20} /></button>
    {#each sources as source}
      <div class="vsa-source-sheet__source">
        <p class="vsa-source-sheet__org">{source.organization}</p>
        {#if source.date}
          <p class="vsa-source-sheet__date">Fecha: {source.date}</p>
        {/if}
        {#if source.url}
          <a href={source.url} target="_blank" rel="noopener" class="vsa-source-sheet__link">Ver fuente original</a>
        {/if}
        {#if source.methodology}
          <p class="vsa-source-sheet__methodology">Basado en {source.methodology}</p>
        {/if}
      </div>
    {/each}
  </dialog>
  ```
- Open/close via `dialogEl.showModal()` / `dialogEl.close()` controlled by `$effect`
- Styles:
  - Position: fixed bottom, slides up
  - Background: `var(--vsa-bg-card)`
  - Border-radius: 16px top corners (`var(--vsa-radius-lg)`)
  - Handle: 4px x 36px centered, `var(--vsa-text-secondary)` at 30% opacity
  - Backdrop: `::backdrop { background: rgba(0, 0, 0, 0.5); }`
  - Organization name: 20px subheading semibold
  - Date: 12px caption, `var(--vsa-text-secondary)`
  - Link: 16px body, `var(--vsa-accent)` color
  - Transition: transform 200ms ease-out open, 150ms ease-in close
  - Padding: `var(--vsa-space-lg)` (24px)

**4. Create `pwa/src/lib/components/TableWrapper.svelte`:**
- Props: `star` (boolean, default false) — per D-19, star tables get elevated treatment
- BEM class: `vsa-table-wrapper` (or `vsa-star-table` if star=true)
- Structure:
  ```html
  <div
    class={star ? 'vsa-star-table' : 'vsa-table-wrapper'}
    role="region"
    aria-label="Tabla con scroll horizontal"
    tabindex="0"
    bind:this={wrapperEl}
  >
    {@render children()}
  </div>
  ```
- Scroll shadow: use `$effect` with scroll event listener on wrapperEl. Show right fade when `scrollWidth > clientWidth && scrollLeft < scrollWidth - clientWidth`. Use CSS `::after` pseudo-element with 32px linear gradient from transparent to `var(--vsa-bg-primary)`.
- Styles for `vsa-table-wrapper`:
  - `overflow-x: auto`
  - `-webkit-overflow-scrolling: touch`
  - Table children: 16px body, header cells 12px caption semibold `var(--vsa-text-secondary)`, cell padding 8px 12px, row border 1px `var(--vsa-border)`
- Styles for `vsa-star-table`:
  - Same scrollable behavior
  - `background: var(--vsa-bg-card)`
  - `box-shadow: var(--vsa-shadow-elevated)`
  - `border-radius: var(--vsa-radius-md)` (12px)
  - `border-left: 3px solid var(--vsa-accent)` (emerald)
  - `padding: var(--vsa-space-md)` (16px)

**5. Create `pwa/src/lib/components/StarTable.svelte`:**
- Simple wrapper that renders TableWrapper with `star={true}`:
  ```html
  <script>
    import TableWrapper from './TableWrapper.svelte';
    let { children } = $props();
  </script>
  <TableWrapper star={true}>
    {@render children()}
  </TableWrapper>
  ```

**6. Create `pwa/src/lib/components/DiagramViewer.svelte`:**
- Props: `src` (string, SVG path), `caption` (string), `type` (string), `complexity` ('simple' | 'complex')
- BEM class: `vsa-diagram`
- Structure:
  ```html
  <figure class="vsa-diagram" data-diagram-type={type}>
    <img src={src} alt={caption || 'Diagrama'} loading="lazy" class="vsa-diagram__img" />
    {#if complexity === 'complex'}
      <button
        class="vsa-diagram__expand"
        aria-label="Ampliar diagrama"
        onclick={openFullscreen}
      >
        <Maximize2 size={20} />
      </button>
    {/if}
    {#if caption}
      <figcaption class="vsa-diagram__caption">{caption}</figcaption>
    {/if}
  </figure>

  <dialog bind:this={dialogEl} class="vsa-diagram__fullscreen">
    <button class="vsa-diagram__fullscreen-close" aria-label="Cerrar" onclick={closeFullscreen}>
      <X size={24} />
    </button>
    <div class="vsa-diagram__fullscreen-content" style="touch-action: pinch-zoom;">
      <img src={src} alt={caption || 'Diagrama'} />
    </div>
  </dialog>
  ```
- Import `Maximize2` and `X` from `@lucide/svelte`
- Use native `<dialog>` for fullscreen (per UI-SPEC: native dialog, no modal library)
- `openFullscreen`: `dialogEl.showModal()`
- `closeFullscreen`: `dialogEl.close()`
- Styles:
  - Inline: `max-width: 100%`, img `width: 100%`
  - Expand button: positioned top-right, 44px touch target, semi-transparent background
  - Caption: 12px caption italic, `var(--vsa-text-secondary)`, gap `var(--vsa-space-sm)`
  - Fullscreen dialog: `var(--vsa-bg-primary)` background, close button top-right 44px
  - Fullscreen content: `overflow: auto`, `touch-action: pinch-zoom` for pinch-zoom on mobile
  - Transition: opacity 150ms ease-out open, 150ms ease-in close (native dialog)
  </action>
  <verify>
    <automated>cd /Users/jw.castillo/Documents/personales/venezuela-sa/pwa && ls src/lib/components/Admonition.svelte src/lib/components/SourceBadge.svelte src/lib/components/SourceSheet.svelte src/lib/components/TableWrapper.svelte src/lib/components/StarTable.svelte src/lib/components/DiagramViewer.svelte && grep -l "vsa-admonition" src/lib/components/Admonition.svelte && grep -l "vsa-source-badge" src/lib/components/SourceBadge.svelte && grep -l "vsa-diagram" src/lib/components/DiagramViewer.svelte && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - pwa/src/lib/components/Admonition.svelte contains `role="note"` and `vsa-admonition`
    - Admonition.svelte contains `AlertTriangle` and `Info` and `Lightbulb` imports
    - Admonition.svelte uses `$props()` (Svelte 5 runes)
    - pwa/src/lib/components/SourceBadge.svelte contains `role="button"` and `vsa-source-badge`
    - SourceBadge.svelte contains `aria-label="Fuente:`
    - pwa/src/lib/components/SourceSheet.svelte contains `<dialog` and `vsa-source-sheet`
    - SourceSheet.svelte contains `Ver fuente original`
    - pwa/src/lib/components/TableWrapper.svelte contains `vsa-table-wrapper` and `role="region"`
    - TableWrapper.svelte contains `overflow-x: auto`
    - pwa/src/lib/components/StarTable.svelte imports TableWrapper with `star={true}`
    - pwa/src/lib/components/DiagramViewer.svelte contains `vsa-diagram` and `<dialog`
    - DiagramViewer.svelte contains `Maximize2` import and `aria-label="Ampliar diagrama"`
    - DiagramViewer.svelte contains `touch-action: pinch-zoom`
    - All components use `$props()` Svelte 5 syntax
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>6 content rendering components created following UI-SPEC exactly. Admonition with type-specific colors/icons. SourceBadge/SourceSheet with full citation flow. TableWrapper/StarTable with scroll + fade shadow. DiagramViewer with fullscreen expand via native dialog. All use Svelte 5 runes and BEM vsa-* namespace.</done>
</task>

<task type="auto">
  <name>Task 3: Create Article, ReadingProgress, ChapterHeader, and ChapterSources components</name>
  <files>pwa/src/lib/components/Article.svelte, pwa/src/lib/components/ReadingProgress.svelte, pwa/src/lib/components/ChapterHeader.svelte, pwa/src/lib/components/ChapterSources.svelte</files>
  <read_first>
    - pwa/src/lib/styles/article.css (created in Task 1)
    - pwa/src/lib/stores/reading-progress.js (created in Plan 01)
    - pwa/src/lib/components/Admonition.svelte (created in Task 2 — pattern reference)
    - .planning/phases/02-content-migration/02-UI-SPEC.md (Article, ReadingProgress, ChapterHeader, ChapterSources specs)
  </read_first>
  <action>
**1. Create `pwa/src/lib/components/Article.svelte`:**
- Purpose: Main container for rendered markdown chapter content (D-01 continuous scroll)
- Props: `docs` (array of `{ component, metadata }`), `chapterSlug` (string)
- Imports article.css
- Structure:
  ```html
  <article class="vsa-article">
    {#each docs as doc, i}
      <section class="vsa-article__section" id="doc-{i}">
        <svelte:component this={doc.component} />
      </section>
    {/each}
  </article>
  ```
- Each doc is an mdsvex-processed Svelte component rendered via `<svelte:component>`
- Sections separated by `vsa-article__section` with border-top between consecutive sections

**2. Create `pwa/src/lib/components/ReadingProgress.svelte`:**
- Props: `percent` (number 0-100)
- BEM class: `vsa-reading-progress`
- Structure:
  ```html
  <div
    class="vsa-reading-progress"
    role="progressbar"
    aria-valuenow={percent}
    aria-valuemin={0}
    aria-valuemax={100}
    aria-label="Progreso de lectura"
  >
    <div class="vsa-reading-progress__fill" style="width: {percent}%"></div>
  </div>
  ```
- Styles:
  - Fixed at very top of viewport: `position: fixed; top: 0; left: 0; right: 0; z-index: 100;`
  - Track: 3px height, `var(--vsa-bg-secondary)` background
  - Fill: 3px height, `var(--vsa-accent)` background
  - Transition: `width 100ms linear`
  - Width controlled by `percent` prop

**3. Create `pwa/src/lib/components/ChapterHeader.svelte`:**
- Props: `title` (string), `readingTime` (number, minutes)
- BEM class: `vsa-chapter-header`
- Structure:
  ```html
  <header class="vsa-chapter-header">
    <h1 class="vsa-chapter-header__title">{title}</h1>
    <div class="vsa-chapter-header__meta">
      <Clock size={12} />
      <span>{readingTime} min de lectura</span>
    </div>
    <hr class="vsa-chapter-header__separator" />
  </header>
  ```
- Import `Clock` from `@lucide/svelte`
- Styles:
  - Title: 28px display semibold (`var(--vsa-font-size-2xl)`)
  - Meta: 12px caption `var(--vsa-text-secondary)`, flex row, gap `var(--vsa-space-xs)`
  - Separator: 1px `var(--vsa-border)`, margin-bottom `var(--vsa-space-lg)` (24px)

**4. Create `pwa/src/lib/components/ChapterSources.svelte`:**
- Props: `sources` (array of `{ organization, type, date, url }`)
- BEM class: `vsa-chapter-sources`
- State: `let expanded = $state(false);`
- Structure (per D-13):
  ```html
  <div class="vsa-chapter-sources">
    <button
      class="vsa-chapter-sources__toggle"
      onclick={() => expanded = !expanded}
      aria-expanded={expanded}
    >
      <span>Fuentes de este capitulo ({sources.length})</span>
      {#if expanded}
        <ChevronUp size={16} />
      {:else}
        <ChevronDown size={16} />
      {/if}
    </button>
    {#if expanded}
      <div class="vsa-chapter-sources__list" transition:slide={{ duration: 200 }}>
        {#each sources as source}
          <div class="vsa-chapter-sources__item">
            <span class="vsa-chapter-sources__org">{source.organization}</span>
            {#if source.date}
              <span class="vsa-chapter-sources__date">{source.date}</span>
            {/if}
            {#if source.url}
              <a href={source.url} target="_blank" rel="noopener">{source.url}</a>
            {/if}
          </div>
        {/each}
      </div>
    {/if}
  </div>
  ```
- Import `ChevronDown`, `ChevronUp` from `@lucide/svelte`
- Use Svelte `transition:slide` for expand/collapse animation
- Styles:
  - Toggle: full-width 48px tap target, 16px body semibold, flex row between
  - List: grouped by source type
  - Org name: 16px body, date: 12px caption `var(--vsa-text-secondary)`
  - Collapsed by default per D-13
  - Top margin: `var(--vsa-space-2xl)` (48px) to separate from chapter content
  </action>
  <verify>
    <automated>cd /Users/jw.castillo/Documents/personales/venezuela-sa/pwa && ls src/lib/components/Article.svelte src/lib/components/ReadingProgress.svelte src/lib/components/ChapterHeader.svelte src/lib/components/ChapterSources.svelte && grep -l "vsa-article" src/lib/components/Article.svelte && grep -l "role=\"progressbar\"" src/lib/components/ReadingProgress.svelte && grep -l "Fuentes de este capitulo" src/lib/components/ChapterSources.svelte && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - pwa/src/lib/components/Article.svelte contains `<article class="vsa-article">`
    - Article.svelte contains `svelte:component` for rendering mdsvex docs
    - Article.svelte contains `vsa-article__section` class for doc sections
    - pwa/src/lib/components/ReadingProgress.svelte contains `role="progressbar"`
    - ReadingProgress.svelte contains `aria-valuenow` and `aria-label="Progreso de lectura"`
    - ReadingProgress.svelte has `position: fixed` and `z-index` in styles
    - ReadingProgress.svelte fill uses `var(--vsa-accent)` color
    - pwa/src/lib/components/ChapterHeader.svelte contains `vsa-chapter-header`
    - ChapterHeader.svelte contains `min de lectura` text
    - ChapterHeader.svelte imports `Clock` from `@lucide/svelte`
    - pwa/src/lib/components/ChapterSources.svelte contains `Fuentes de este capitulo`
    - ChapterSources.svelte contains `aria-expanded` attribute
    - ChapterSources.svelte imports `ChevronDown` and `ChevronUp`
    - All 4 components use `$props()` Svelte 5 syntax
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>4 container/structural components created. Article renders continuous scroll chapters. ReadingProgress tracks scroll as fixed bar. ChapterHeader shows title + reading time. ChapterSources provides collapsible source list. All follow UI-SPEC accessibility and design requirements.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/jw.castillo/Documents/personales/venezuela-sa/pwa
# Verify all 10 components exist
ls src/lib/components/{Article,Admonition,SourceBadge,SourceSheet,TableWrapper,StarTable,DiagramViewer,ReadingProgress,ChapterHeader,ChapterSources}.svelte
# Verify tokens extended
grep "vsa-source-multilateral" src/lib/styles/tokens.css
grep "vsa-info" src/lib/styles/tokens.css
# Run tests
npx vitest run
```
</verification>

<success_criteria>
- 10 new Svelte components created following UI-SPEC component inventory
- tokens.css extended with source badge colors (5 types x 2 modes) and admonition colors
- article.css provides complete typography mapping for markdown content
- All components use Svelte 5 runes ($props, $state, $effect)
- All components use BEM vsa-* namespace
- All interactive elements have 44px minimum touch targets
- All components have proper ARIA attributes per accessibility spec
- Tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-migration/02-02-SUMMARY.md`
</output>
