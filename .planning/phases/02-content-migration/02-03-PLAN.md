---
phase: 02-content-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - pwa/src/routes/plan/+page.svelte
  - pwa/src/routes/plan/[chapter]/+page.js
  - pwa/src/routes/plan/[chapter]/+page.svelte
  - pwa/src/lib/components/ChapterCard.svelte
  - pwa/src/lib/components/ChapterNav.svelte
  - pwa/src/lib/components/ContinueReading.svelte
  - pwa/src/lib/components/PlanLanding.svelte
  - pwa/src/lib/components/SearchBar.svelte
  - pwa/src/routes/plan/glosario/+page.js
  - pwa/src/routes/plan/glosario/+page.svelte
  - pwa/src/routes/plan/referencias/+page.js
  - pwa/src/routes/plan/referencias/+page.svelte
autonomous: true
requirements:
  - CONT-01
  - CONT-07
  - CONT-08
  - CONT-06

must_haves:
  truths:
    - "User can browse the El Plan landing page with chapter cards showing icon, title, description, hook stat, and reading time"
    - "User can tap a chapter card and read the full chapter as continuous scroll"
    - "User can navigate between chapters with previous/next buttons"
    - "User can search across all content and get results while offline"
    - "User can share a direct URL to any section and the recipient lands on that exact content"
    - "User sees Continue Reading CTA if they have prior progress"
  artifacts:
    - path: "pwa/src/routes/plan/+page.svelte"
      provides: "El Plan landing page (book index)"
      contains: "PlanLanding"
    - path: "pwa/src/routes/plan/[chapter]/+page.svelte"
      provides: "Chapter reader page with continuous scroll"
      contains: "Article"
    - path: "pwa/src/routes/plan/[chapter]/+page.js"
      provides: "Chapter data loader (aggregates docs per chapter)"
      exports: ["load"]
    - path: "pwa/src/lib/components/ChapterCard.svelte"
      provides: "Chapter card for landing page"
      contains: "vsa-chapter-card"
    - path: "pwa/src/lib/components/ChapterNav.svelte"
      provides: "Previous/Next chapter navigation"
      contains: "vsa-chapter-nav"
    - path: "pwa/src/lib/components/SearchBar.svelte"
      provides: "Pagefind-powered search with lazy loading"
      contains: "vsa-search"
    - path: "pwa/src/lib/components/PlanLanding.svelte"
      provides: "Full landing page layout"
      contains: "vsa-plan-landing"
  key_links:
    - from: "pwa/src/routes/plan/[chapter]/+page.js"
      to: "pwa/src/lib/content/chapters.js"
      via: "import chapters"
      pattern: "chapters.find"
    - from: "pwa/src/routes/plan/[chapter]/+page.svelte"
      to: "pwa/src/lib/components/Article.svelte"
      via: "renders chapter docs"
      pattern: "Article"
    - from: "pwa/src/lib/components/SearchBar.svelte"
      to: "/pagefind/pagefind.js"
      via: "dynamic import on focus"
      pattern: "import.*pagefind"
    - from: "pwa/src/lib/components/ChapterNav.svelte"
      to: "pwa/src/lib/content/chapters.js"
      via: "chapter ordering for prev/next"
      pattern: "chapters"
---

<objective>
Build the complete book experience: El Plan landing page with chapter cards, chapter reader with continuous scroll + scroll tracking + deep links, previous/next chapter navigation, Pagefind search integration, and Continue Reading CTA. Also handle standalone pages (glosario, referencias).

Purpose: This is where the user actually reads the plan. Plans 01-02 built the pipeline and components; this plan wires them into the SvelteKit routes that users navigate to. After this plan, a citizen can read the entire plan as a book.
Output: Working book navigation with landing page, chapter reader, search, and all route wiring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-migration/02-CONTEXT.md
@.planning/phases/02-content-migration/02-UI-SPEC.md
@.planning/phases/02-content-migration/02-RESEARCH.md

<interfaces>
From pwa/src/lib/content/chapters.js (created in Plan 01):
```javascript
export const chapters = [
  { slug: 'introduccion', title: 'Venezuela S.A.', icon: 'BookOpen', description: '...', hookStat: '40M accionistas', docs: ['intro'], visible: true, isSeparateSection: false },
  { slug: 'fundamentos', title: 'Fundamentos', ... },
  // ... 10 chapters total
];
export const hiddenChapters = ['08-pitch', '09-investors'];
export const standalonePages = [
  { slug: 'glosario', title: 'Glosario', doc: 'glosario' },
  { slug: 'referencias', title: 'Referencias', doc: 'referencias' },
];
```

From pwa/src/lib/stores/reading-progress.js (created in Plan 01):
```javascript
export const readingProgress: Writable;
export const lastReadChapter: Readable;
export function updateChapterProgress(chapterSlug, scrollPercent, scrollY): void;
```

From pwa/src/lib/utils/reading-time.js (created in Plan 01):
```javascript
export function calculateReadingTime(text): number;
```

From Plan 02 components:
- Article.svelte: `<Article docs={docs} chapterSlug={slug} />`
- ReadingProgress.svelte: `<ReadingProgress percent={scrollPercent} />`
- ChapterHeader.svelte: `<ChapterHeader title={title} readingTime={minutes} />`
- ChapterSources.svelte: `<ChapterSources sources={sources} />`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chapter reader route with continuous scroll, deep links, and scroll tracking</name>
  <files>pwa/src/routes/plan/[chapter]/+page.js, pwa/src/routes/plan/[chapter]/+page.svelte, pwa/src/lib/components/ChapterNav.svelte, pwa/src/routes/plan/glosario/+page.js, pwa/src/routes/plan/glosario/+page.svelte, pwa/src/routes/plan/referencias/+page.js, pwa/src/routes/plan/referencias/+page.svelte</files>
  <read_first>
    - pwa/src/routes/plan/+page.svelte (current ComingSoon placeholder)
    - pwa/src/lib/content/chapters.js (chapter definitions — created in Plan 01)
    - pwa/src/lib/components/Article.svelte (created in Plan 02)
    - pwa/src/lib/components/ReadingProgress.svelte (created in Plan 02)
    - pwa/src/lib/components/ChapterHeader.svelte (created in Plan 02)
    - pwa/src/lib/components/ChapterSources.svelte (created in Plan 02)
    - pwa/src/lib/stores/reading-progress.js (created in Plan 01)
    - .planning/phases/02-content-migration/02-UI-SPEC.md (chapter reader layout, deep link behavior, scroll behavior)
    - .planning/phases/02-content-migration/02-CONTEXT.md (D-01 through D-05)
  </read_first>
  <action>
**1. Create `pwa/src/routes/plan/[chapter]/+page.js`:**

```javascript
import { chapters } from '$lib/content/chapters.js';
import { error } from '@sveltejs/kit';
import { calculateReadingTime } from '$lib/utils/reading-time.js';

export async function load({ params }) {
  const chapterIndex = chapters.findIndex(c => c.slug === params.chapter);
  if (chapterIndex === -1) throw error(404, 'Capitulo no encontrado');

  const chapter = chapters[chapterIndex];

  // Load all docs for this chapter (mdsvex preprocessed .md files)
  const docs = await Promise.all(
    chapter.docs.map(async (docPath) => {
      try {
        // Dynamic import from docs directory (symlinked/copied to content/)
        const mod = await import(`../../../content/${docPath}.md`);
        return {
          component: mod.default,
          metadata: mod.metadata || {},
        };
      } catch (e) {
        console.error(`Failed to load doc: ${docPath}`, e);
        return null;
      }
    })
  );

  const validDocs = docs.filter(Boolean);

  // Compute reading time from all doc metadata or raw text
  // Note: reading-time lib is used at build time via mdsvex; here we sum
  const totalReadingTime = validDocs.reduce((sum, doc) => {
    return sum + (doc.metadata.readingTime || 5); // fallback 5 min per doc
  }, 0);

  // Prev/next chapters (only visible, per D-06)
  const visibleChapters = chapters.filter(c => c.visible);
  const visibleIndex = visibleChapters.findIndex(c => c.slug === params.chapter);
  const prevChapter = visibleIndex > 0 ? visibleChapters[visibleIndex - 1] : null;
  const nextChapter = visibleIndex < visibleChapters.length - 1 ? visibleChapters[visibleIndex + 1] : null;

  return {
    chapter,
    docs: validDocs,
    readingTime: totalReadingTime,
    prevChapter,
    nextChapter,
  };
}

export const prerender = true;
```

The import path `../../../content/${docPath}.md` requires that markdown docs are accessible from the pwa/src directory. Create a content/ directory strategy:
- Option A: Symlink `pwa/src/content/` -> `../../docs/` (works for dev + build)
- Option B: Copy docs into `pwa/src/content/` at build time
- Use Option A (symlink) — simpler, keeps docs as single source of truth. Add to build:mermaid script or create a separate setup step.

**2. Create `pwa/src/routes/plan/[chapter]/+page.svelte`:**

```svelte
<script>
  import { browser } from '$app/environment';
  import { page } from '$app/state';
  import Article from '$lib/components/Article.svelte';
  import ReadingProgress from '$lib/components/ReadingProgress.svelte';
  import ChapterHeader from '$lib/components/ChapterHeader.svelte';
  import ChapterNav from '$lib/components/ChapterNav.svelte';
  import ChapterSources from '$lib/components/ChapterSources.svelte';
  import { updateChapterProgress, readingProgress } from '$lib/stores/reading-progress.js';

  let { data } = $props();
  let scrollPercent = $state(0);
  let ticking = $state(false);

  // Scroll tracking per D-03
  function trackScroll() {
    if (!browser) return;
    const docHeight = document.documentElement.scrollHeight - window.innerHeight;
    if (docHeight <= 0) return;
    scrollPercent = Math.round((window.scrollY / docHeight) * 100);
    updateChapterProgress(data.chapter.slug, scrollPercent / 100, window.scrollY);
  }

  function onScroll() {
    if (!ticking) {
      requestAnimationFrame(() => {
        trackScroll();
        ticking = false;
      });
      ticking = true;
    }
  }

  // Deep link scroll per UI-SPEC: scroll to hash with 80px offset
  $effect(() => {
    if (!browser) return;

    window.addEventListener('scroll', onScroll, { passive: true });

    // Handle hash-based deep linking
    if (window.location.hash) {
      const target = document.querySelector(window.location.hash);
      if (target) {
        setTimeout(() => {
          const y = target.getBoundingClientRect().top + window.scrollY - 80;
          window.scrollTo({ top: y, behavior: 'instant' });
          target.focus();
        }, 100);
      }
    }

    // Resume reading position per D-05
    const progress = $readingProgress[data.chapter.slug];
    if (progress && !window.location.hash) {
      window.scrollTo({ top: progress.lastScrollY, behavior: 'instant' });
    }

    return () => {
      window.removeEventListener('scroll', onScroll);
    };
  });
</script>

<ReadingProgress percent={scrollPercent} />

<div class="vsa-chapter-reader" style="overscroll-behavior-y: contain;">
  <ChapterHeader title={data.chapter.title} readingTime={data.readingTime} />

  <Article docs={data.docs} chapterSlug={data.chapter.slug} />

  <!-- Collapsible chapter sources at bottom per D-13 -->
  <!-- Sources are extracted from doc metadata during build -->
  {#if data.docs.some(d => d.metadata.sources?.length)}
    <ChapterSources sources={data.docs.flatMap(d => d.metadata.sources || [])} />
  {/if}

  <ChapterNav
    prevChapter={data.prevChapter}
    nextChapter={data.nextChapter}
  />
</div>
```

**3. Create `pwa/src/lib/components/ChapterNav.svelte`:**
- Props: `prevChapter` (object | null), `nextChapter` (object | null)
- BEM class: `vsa-chapter-nav`
- Structure per UI-SPEC:
  ```html
  <nav class="vsa-chapter-nav" aria-label="Capitulos">
    {#if prevChapter}
      <a href="/plan/{prevChapter.slug}" class="vsa-chapter-nav__prev">
        <ChevronLeft size={20} />
        <div>
          <span class="vsa-chapter-nav__label">Anterior</span>
          <span class="vsa-chapter-nav__title">{prevChapter.title}</span>
        </div>
      </a>
    {:else}
      <div></div>
    {/if}
    {#if nextChapter}
      <a href="/plan/{nextChapter.slug}" class="vsa-chapter-nav__next">
        <div>
          <span class="vsa-chapter-nav__label">Siguiente</span>
          <span class="vsa-chapter-nav__title">{nextChapter.title}</span>
        </div>
        <ChevronRight size={20} />
      </a>
    {:else}
      <div></div>
    {/if}
  </nav>
  ```
- Import `ChevronLeft`, `ChevronRight` from `@lucide/svelte`
- Styles:
  - Display: flex, justify-content: space-between
  - Background: `var(--vsa-bg-secondary)`
  - Border-top: 1px `var(--vsa-border)`
  - Padding: `var(--vsa-space-md)` (16px)
  - Min-height: 56px (matches tab bar)
  - Touch target: each side 50% width x 56px
  - Text: 16px body `var(--vsa-text-primary)`, label is 12px caption `var(--vsa-text-secondary)`
  - Transition: background-color 120ms ease on hover/press

**4. Create standalone page routes for glosario and referencias:**

`pwa/src/routes/plan/glosario/+page.js`:
```javascript
export async function load() {
  const mod = await import('../../../../content/glosario.md');
  return { component: mod.default, metadata: mod.metadata || {}, title: 'Glosario' };
}
export const prerender = true;
```

`pwa/src/routes/plan/glosario/+page.svelte`:
```svelte
<script>
  import Article from '$lib/components/Article.svelte';
  let { data } = $props();
</script>
<div class="vsa-chapter-reader">
  <h1 style="font-size: var(--vsa-font-size-2xl); font-weight: 600; padding: var(--vsa-space-3xl) var(--vsa-space-md) var(--vsa-space-lg);">{data.title}</h1>
  <Article docs={[{ component: data.component, metadata: data.metadata }]} chapterSlug="glosario" />
</div>
```

Same pattern for `pwa/src/routes/plan/referencias/` using `referencias.md`.

**5. Create content symlink:**
```bash
cd pwa/src && ln -sf ../../../docs content
```
This makes `pwa/src/content/` point to the root `docs/` directory so that `import('../../../content/intro.md')` resolves to the actual docs. The symlink must be relative for portability.

Note: If symlink causes issues with adapter-static or Vite, fall back to a build-time copy script. Test by running `npm run build` after creating the symlink.
  </action>
  <verify>
    <automated>cd /Users/jw.castillo/Documents/personales/venezuela-sa/pwa && test -d src/routes/plan/\[chapter\] && test -f src/routes/plan/\[chapter\]/+page.js && test -f src/routes/plan/\[chapter\]/+page.svelte && test -f src/lib/components/ChapterNav.svelte && grep -l "prerender" src/routes/plan/\[chapter\]/+page.js && grep -l "vsa-chapter-nav" src/lib/components/ChapterNav.svelte && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - pwa/src/routes/plan/[chapter]/+page.js exists with `export async function load` and `export const prerender = true`
    - +page.js imports from `$lib/content/chapters.js` and uses `chapters.findIndex`
    - +page.js computes `prevChapter` and `nextChapter` from visible chapters only
    - +page.js dynamically imports docs via `import(` with content path
    - pwa/src/routes/plan/[chapter]/+page.svelte imports Article, ReadingProgress, ChapterHeader, ChapterNav, ChapterSources
    - +page.svelte has scroll tracking with `requestAnimationFrame` throttle
    - +page.svelte handles hash-based deep linking with 80px offset
    - +page.svelte handles "resume reading" from stored scrollY position
    - +page.svelte has `overscroll-behavior-y: contain`
    - pwa/src/lib/components/ChapterNav.svelte contains `vsa-chapter-nav` and `aria-label="Capitulos"`
    - ChapterNav.svelte contains `ChevronLeft` and `ChevronRight` imports
    - ChapterNav.svelte renders links to `/plan/{slug}`
    - pwa/src/routes/plan/glosario/+page.js exists with prerender
    - pwa/src/routes/plan/referencias/+page.js exists with prerender
    - Content symlink or copy mechanism exists (pwa/src/content/ points to docs/)
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>Chapter reader route fully wired: loads all docs for a chapter via mdsvex dynamic imports, renders as continuous scroll with Article component, tracks scroll position to reading-progress store, handles deep link navigation with 80px offset, provides previous/next chapter navigation. Standalone glosario and referencias pages created. Content accessible via symlink.</done>
</task>

<task type="auto">
  <name>Task 2: Create El Plan landing page with chapter cards, search bar, continue reading, and install Pagefind</name>
  <files>pwa/src/routes/plan/+page.svelte, pwa/src/lib/components/PlanLanding.svelte, pwa/src/lib/components/ChapterCard.svelte, pwa/src/lib/components/ContinueReading.svelte, pwa/src/lib/components/SearchBar.svelte, pwa/package.json</files>
  <read_first>
    - pwa/src/routes/plan/+page.svelte (current ComingSoon placeholder — will be replaced)
    - pwa/src/lib/content/chapters.js (chapter definitions)
    - pwa/src/lib/stores/reading-progress.js (reading progress store)
    - .planning/phases/02-content-migration/02-UI-SPEC.md (landing page layout, chapter card layout, search result item, copywriting contract)
    - .planning/phases/02-content-migration/02-CONTEXT.md (D-21 through D-29)
    - .planning/phases/02-content-migration/02-RESEARCH.md (Pagefind integration pattern)
  </read_first>
  <action>
**1. Install Pagefind:**
```bash
cd pwa && npm install -D pagefind
```

Add to package.json scripts:
- `"build:search": "npx pagefind --site build --glob '**/*.html'"`
- Update `"build"` to: `"npm run build:mermaid && vite build && npm run build:search"`

The build order is: mermaid SVGs -> SvelteKit build -> Pagefind indexes the built HTML.

**2. Create `pwa/src/lib/components/SearchBar.svelte`:**
- BEM class: `vsa-search`
- Svelte 5 runes: `let query = $state('');`, `let results = $state([]);`, `let pagefind = $state(null);`
- Structure per UI-SPEC SearchBar spec:
  ```html
  <div class="vsa-search" role="search">
    <input
      type="search"
      placeholder="Buscar en el plan..."
      bind:value={query}
      onfocus={initPagefind}
      oninput={search}
      class="vsa-search__input"
      aria-label="Buscar en el plan"
    />
    {#if results.length > 0}
      <ul class="vsa-search__results" role="listbox" aria-live="polite">
        {#each results as result}
          <li class="vsa-search__result">
            <a href={result.url}>
              <span class="vsa-search__result-title">{result.meta?.title || result.url}</span>
              <p class="vsa-search__result-excerpt">{@html result.excerpt}</p>
              <span class="vsa-search__result-url">{result.url}</span>
            </a>
          </li>
        {/each}
      </ul>
    {:else if query.trim().length > 0 && pagefind}
      <div class="vsa-search__empty">
        <p class="vsa-search__empty-heading">Sin resultados</p>
        <p class="vsa-search__empty-body">Intenta con otros terminos o navega por los capitulos.</p>
      </div>
    {/if}
  </div>
  ```
- `initPagefind`: lazy-load on focus `pagefind = await import(/* @vite-ignore */ '/pagefind/pagefind.js'); await pagefind.init();`
- `search`: debounce 200ms, call `pagefind.search(query)`, take first 10 results, resolve `.data()` for each
- Styles:
  - Input: full-width, 48px height, `var(--vsa-bg-card)` background, `var(--vsa-border)` border, `var(--vsa-radius-md)` border-radius, 16px text, placeholder `var(--vsa-text-secondary)`
  - Results dropdown: `var(--vsa-bg-card)` background, `var(--vsa-shadow-elevated)` shadow, max-height 60vh, overflow-y auto
  - Result item: padding 12px 16px, title 16px semibold, excerpt 16px `var(--vsa-text-secondary)` with `<mark>` in `var(--vsa-accent)`, URL 12px caption, divider 1px `var(--vsa-border)`
  - Transitions: dropdown appears with opacity+translateY 150ms ease-out
  - On mobile: results overlay the landing page content

**3. Create `pwa/src/lib/components/ChapterCard.svelte`:**
- Props: `slug`, `title`, `icon` (string name), `description`, `hookStat`, `readingTime`, `progress`, `isStartHere`, `isSeparateSection`
- BEM class: `vsa-chapter-card`
- Import Lucide icons dynamically by name: use a lookup object `{ BookOpen, Building2, Banknote, Users, Shield, Rocket, Zap, CalendarCheck, Flag, Lightbulb }` from `@lucide/svelte`
- Structure per UI-SPEC card layout:
  ```html
  <a href="/plan/{slug}" class="vsa-chapter-card" class:vsa-chapter-card--in-progress={progress > 0 && progress < 100}>
    <div class="vsa-chapter-card__header">
      <svelte:component this={iconMap[icon]} size={24} class="vsa-chapter-card__icon" />
      <span class="vsa-chapter-card__title">{title}</span>
      <span class="vsa-chapter-card__badge">
        {#if isStartHere}
          Empieza aqui
        {:else if progress > 0}
          {progress}% leido
        {:else}
          Nuevo
        {/if}
      </span>
    </div>
    <p class="vsa-chapter-card__description">{description}</p>
    <div class="vsa-chapter-card__footer">
      <span class="vsa-chapter-card__stat">{hookStat}</span>
      <span class="vsa-chapter-card__time">
        <Clock size={12} />
        {readingTime} min
      </span>
    </div>
  </a>
  ```
- Import `Clock` from `@lucide/svelte`
- Styles per UI-SPEC:
  - Card: `var(--vsa-bg-card)` background, `var(--vsa-radius-md)` border-radius, `var(--vsa-shadow-card)` shadow, 16px padding
  - Icon: 24px, `var(--vsa-accent)` color
  - Title: 20px subheading semibold
  - Description: 16px body, `var(--vsa-text-secondary)`
  - Hook stat: 28px display semibold, `var(--vsa-accent)` color
  - Reading time: 12px caption, `var(--vsa-text-secondary)`
  - Badge: 12px caption, `var(--vsa-accent-subtle)` background
  - "Empieza aqui" badge: `var(--vsa-accent)` background, white text
  - Touch: entire card tappable
  - Active state: `transform: scale(0.98)` 100ms ease-in-out

**4. Create `pwa/src/lib/components/ContinueReading.svelte`:**
- Props: `chapterSlug` (string), `chapterTitle` (string), `progress` (number)
- BEM class: `vsa-continue-reading`
- Structure:
  ```html
  <a href="/plan/{chapterSlug}" class="vsa-continue-reading">
    <span class="vsa-continue-reading__label">Continuar leyendo</span>
    <span class="vsa-continue-reading__detail">{chapterTitle} - {progress}% leido</span>
  </a>
  ```
- Styles:
  - Full-width pill button: `var(--vsa-accent)` background, white text
  - Label: 16px body semibold
  - Detail: 12px caption below
  - Border-radius: `var(--vsa-radius-full)` (pill)
  - Min-height: 48px

**5. Create `pwa/src/lib/components/PlanLanding.svelte`:**
- Main landing page component assembling all sub-components
- Imports: `SearchBar`, `ChapterCard`, `ContinueReading`, `chapters` from chapters.js, `readingProgress`, `lastReadChapter`
- Structure per UI-SPEC landing page layout:
  ```html
  <div class="vsa-plan-landing">
    <SearchBar />

    <header class="vsa-plan-landing__hero">
      <h1>El plan completo</h1>
      <p>{mainChapters.length} capitulos, ~{totalReadingTime} min de lectura</p>
      {#if hasAnyProgress}
        <div class="vsa-plan-landing__global-progress">
          <div class="vsa-plan-landing__global-bar" style="width: {globalPercent}%"></div>
          <span>{globalPercent}% leido</span>
        </div>
      {/if}
    </header>

    {#if $lastReadChapter}
      <ContinueReading
        chapterSlug={$lastReadChapter[0]}
        chapterTitle={chapterTitleFor($lastReadChapter[0])}
        progress={$lastReadChapter[1].scrollPercent}
      />
    {:else}
      <a href="/plan/introduccion" class="vsa-continue-reading">
        <span class="vsa-continue-reading__label">Empezar a leer</span>
      </a>
    {/if}

    <div class="vsa-plan-landing__chapters">
      {#each mainChapters as chapter}
        <ChapterCard
          slug={chapter.slug}
          title={chapter.title}
          icon={chapter.icon}
          description={chapter.description}
          hookStat={chapter.hookStat}
          readingTime={chapterReadingTimes[chapter.slug] || 15}
          progress={$readingProgress[chapter.slug]?.scrollPercent || 0}
          isStartHere={chapter.slug === 'fundamentos' && !hasAnyProgress}
        />
      {/each}
    </div>

    <!-- Oportunidades section per D-26 -->
    <div class="vsa-plan-landing__opportunities">
      <h2>Oportunidades de Inversion</h2>
      {#each separateChapters as chapter}
        <ChapterCard
          slug={chapter.slug}
          title={chapter.title}
          icon={chapter.icon}
          description={chapter.description}
          hookStat={chapter.hookStat}
          readingTime={chapterReadingTimes[chapter.slug] || 30}
          progress={$readingProgress[chapter.slug]?.scrollPercent || 0}
          isSeparateSection={true}
        />
      {/each}
    </div>

    <!-- Secondary links per D-27 -->
    <div class="vsa-plan-landing__secondary">
      <a href="/plan/glosario">Glosario</a>
      <a href="/plan/referencias">Referencias</a>
    </div>
  </div>
  ```
- Computed values:
  - `mainChapters = chapters.filter(c => c.visible && !c.isSeparateSection)`
  - `separateChapters = chapters.filter(c => c.visible && c.isSeparateSection)` (oportunidades)
  - `hasAnyProgress = Object.keys($readingProgress).length > 0`
  - `globalPercent = Math.round(average of all chapter progresses)`
- Styles per UI-SPEC landing layout:
  - Hero: 28px display headline, 12px caption subtitle, 3px progress bar
  - Chapter cards: single column on mobile, 2-column on 480px+
  - Opportunities section: section header 20px, separated below main chapters
  - Secondary links: 16px body, `var(--vsa-text-secondary)` color, horizontal layout

**6. Replace `pwa/src/routes/plan/+page.svelte`:**
Remove ComingSoon placeholder. Replace with:
```svelte
<script>
  import PlanLanding from '$lib/components/PlanLanding.svelte';
</script>
<PlanLanding />
```
  </action>
  <verify>
    <automated>cd /Users/jw.castillo/Documents/personales/venezuela-sa/pwa && grep -l "PlanLanding" src/routes/plan/+page.svelte && grep -l "vsa-chapter-card" src/lib/components/ChapterCard.svelte && grep -l "vsa-search" src/lib/components/SearchBar.svelte && grep -l "Continuar leyendo" src/lib/components/ContinueReading.svelte && grep -l "pagefind" src/lib/components/SearchBar.svelte && grep "build:search" package.json && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - pwa/src/routes/plan/+page.svelte no longer contains `ComingSoon` — imports `PlanLanding` instead
    - pwa/src/lib/components/PlanLanding.svelte contains `vsa-plan-landing` and `El plan completo`
    - PlanLanding.svelte contains `Oportunidades de Inversion` section per D-26
    - PlanLanding.svelte contains `Glosario` and `Referencias` secondary links per D-27
    - PlanLanding.svelte contains `Empieza aqui` badge logic for fundamentos per D-28
    - PlanLanding.svelte contains `Empezar a leer` and `Continuar leyendo` CTAs per D-24
    - pwa/src/lib/components/ChapterCard.svelte contains `vsa-chapter-card` and `hookStat`
    - ChapterCard.svelte contains `Empieza aqui` and `leido` and `Nuevo` badge states
    - ChapterCard.svelte renders hook stat in 28px display font size
    - pwa/src/lib/components/SearchBar.svelte contains `pagefind` dynamic import
    - SearchBar.svelte contains `role="search"` and `role="listbox"`
    - SearchBar.svelte contains `Sin resultados` empty state
    - SearchBar.svelte placeholder is `Buscar en el plan...`
    - pwa/src/lib/components/ContinueReading.svelte contains `Continuar leyendo`
    - pwa/package.json contains `"build:search"` and `"pagefind"` in devDependencies
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>El Plan landing page replaces ComingSoon placeholder. Chapter cards show icons, descriptions, hook stats, reading times, and progress badges. SearchBar lazy-loads Pagefind on focus. ContinueReading CTA appears when user has prior progress. Oportunidades as separate section. Glosario/Referencias as secondary links. Pagefind integrated into build pipeline.</done>
</task>

<task type="auto">
  <name>Task 3: Update service worker config, run validation build, and fix content linking</name>
  <files>pwa/vite.config.js</files>
  <read_first>
    - pwa/vite.config.js (current Workbox config)
    - .planning/phases/02-content-migration/02-RESEARCH.md (Pitfall 7 — service worker caching, Workbox config update)
  </read_first>
  <action>
**1. Update Workbox globPatterns in `pwa/vite.config.js`:**

In the SvelteKitPWA workbox config, update `globPatterns` to include content assets:
```javascript
workbox: {
  globPatterns: [
    'client/**/*.{js,css,ico,png,svg,webp,woff2}',
    'prerendered/**/*.html',
    'diagrams/**/*.svg',
    'pagefind/**/*.{js,wasm,pf_meta,pf_index,pf_fragment}',
  ],
  navigateFallback: '200.html'
}
```

The additions are:
- `'diagrams/**/*.svg'` — cached Mermaid SVGs for offline viewing
- `'pagefind/**/*.{js,wasm,pf_meta,pf_index,pf_fragment}'` — search index for offline search

**2. Validate the full build pipeline:**
```bash
cd pwa
npm run build
```

This runs: `build:mermaid && vite build && build:search`

Expected: SvelteKit builds all routes (landing + chapter routes + glosario + referencias), Pagefind indexes the output HTML.

**3. If build fails due to content symlink issues:**
Replace symlink with a build-time copy approach:
- Create `pwa/scripts/copy-content.js` that copies `../docs/` to `pwa/src/content/`
- Add `"prebuild": "node scripts/copy-content.js"` to package.json scripts
- Add `src/content/` to `.gitignore` in pwa/

**4. Run bundle size check:**
```bash
cd pwa && npm run check:bundle
```

Verify JS bundle is still under 200KB gzipped. Per RESEARCH.md Pitfall 5, current usage is 63KB (97KB remaining budget). The new components add client JS but all remark/rehype plugins are build-time only. Pagefind client is lazy-loaded (~6-10KB) and does not count toward initial bundle.
  </action>
  <verify>
    <automated>cd /Users/jw.castillo/Documents/personales/venezuela-sa/pwa && grep "diagrams" vite.config.js && grep "pagefind" vite.config.js && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - pwa/vite.config.js workbox.globPatterns contains `'diagrams/**/*.svg'`
    - pwa/vite.config.js workbox.globPatterns contains `'pagefind/**/*.{js,wasm,pf_meta,pf_index,pf_fragment}'`
    - All existing workbox patterns are preserved (client/**, prerendered/**)
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>Service worker caches Mermaid SVGs and Pagefind search index for full offline support. Build pipeline runs end-to-end: Mermaid SVGs -> SvelteKit build -> Pagefind indexing. Bundle size within budget.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/jw.castillo/Documents/personales/venezuela-sa/pwa
# Verify routes exist
ls src/routes/plan/+page.svelte src/routes/plan/\[chapter\]/+page.{js,svelte}
# Verify components exist
ls src/lib/components/{PlanLanding,ChapterCard,ChapterNav,ContinueReading,SearchBar}.svelte
# Verify Workbox config
grep -A5 "globPatterns" vite.config.js
# Run tests
npx vitest run
# Attempt build (may fail if content symlink not set up yet — that's expected to be resolved during execution)
npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
- Chapter reader route ([chapter]/+page.svelte) renders continuous scroll with all chapter docs
- Landing page shows all chapter cards with hook stats, reading times, progress badges
- Search bar lazy-loads Pagefind and returns results
- Chapter navigation shows previous/next with correct ordering
- Deep links scroll to correct element with 80px offset
- Continue Reading CTA appears with prior progress
- Empieza Aqui badge on Fundamentos for new users (D-28)
- Service worker caches diagrams and search index for offline
- Standalone glosario and referencias pages accessible
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-migration/02-03-SUMMARY.md`
</output>
