---
phase: 02-content-migration
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified: []
autonomous: false
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - CONT-07
  - CONT-08

must_haves:
  truths:
    - "User can browse all plan documents within the El Plan tab"
    - "User can search any term and get results while offline"
    - "User can share a direct URL to any section"
    - "Admonitions, diagrams, and tables render correctly on 360px"
    - "Every data point shows its source"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete content migration. Run the full build, verify all 5 success criteria from the ROADMAP, and confirm the reading experience works end-to-end on a simulated mobile device.

Purpose: Catch visual bugs, broken navigation, and rendering issues that automated tests cannot detect. This is the quality gate before Phase 2 is marked complete.
Output: Verification pass/fail with specific issues for gap closure if needed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-migration/02-CONTEXT.md
@.planning/phases/02-content-migration/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build pipeline and verify automated checks</name>
  <files></files>
  <read_first>
    - pwa/package.json (build scripts)
    - pwa/scripts/check-bundle-size.js (bundle size gate)
    - .planning/phases/02-content-migration/02-VALIDATION.md (test map)
  </read_first>
  <action>
**1. Run the complete build pipeline:**
```bash
cd pwa && npm run build
```

This should execute: build:mermaid -> vite build -> build:search

**2. Run all tests:**
```bash
cd pwa && npx vitest run
```

All 8 test files should pass (real tests + pending todos).

**3. Run bundle size check:**
```bash
cd pwa && npm run check:bundle
```

Verify < 200KB gzipped.

**4. Verify build output:**
- Check that `pwa/build/` contains HTML files for all chapter routes
- Check that `pwa/build/pagefind/` contains search index files
- Check that `pwa/build/diagrams/` or `pwa/static/diagrams/` contains SVG files

**5. Start preview server:**
```bash
cd pwa && npm run preview
```

Report the URL (typically http://localhost:4173) for the human verification task.
  </action>
  <verify>
    <automated>cd /Users/jw.castillo/Documents/personales/venezuela-sa/pwa && npm run build && npx vitest run && npm run check:bundle</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 without errors
    - `npx vitest run` exits 0
    - `npm run check:bundle` exits 0 (bundle < 200KB gzipped)
    - pwa/build/ directory contains HTML files
    - Preview server starts successfully
  </acceptance_criteria>
  <done>Full build pipeline passes. Tests green. Bundle within budget. Preview server running for manual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete content migration experience</name>
  <action>
This is a human verification checkpoint. The executor pauses here and presents the verification steps to the user.

**What was built:** Complete content migration — 85 markdown documents rendered as a navigable book with chapter cards, continuous scroll reading, search, deep links, source attribution badges, admonitions, Mermaid diagrams (static SVG), scrollable tables, and previous/next navigation. All within the existing PWA shell.

**How to verify:**
1. Open http://localhost:4173 (or the preview URL from Task 1)
2. Navigate to "El Plan" tab — verify the landing page shows:
   - "El plan completo" hero with chapter count and reading time
   - Chapter cards with icons, descriptions, hook stats (e.g., "303B barriles"), reading times
   - "Empieza aqui" badge on Fundamentos (or "Empezar a leer" CTA if fresh)
   - Oportunidades section below main chapters
   - Glosario and Referencias secondary links at bottom
   - Search bar at top with "Buscar en el plan..." placeholder
3. Tap on Fundamentos chapter — verify:
   - Chapter title "Fundamentos" with reading time
   - Continuous scroll through all 3 docs (tesis-central, diagnostico, fase-0-emergencia)
   - Admonitions render with colored left border and icon (check for :::danger, :::tip, :::info blocks)
   - Tables scroll horizontally with fade shadow on right edge
   - Source badges appear as colored chips (blue for multilateral, green for energy)
   - Mermaid diagrams render as static SVG images (no loading spinner, no JS error)
   - Reading progress bar at top tracks scroll position
4. Tap a source badge — verify bottom sheet opens with organization name, date, URL link
5. Scroll to a complex diagram — verify "expand" button appears and fullscreen works with pinch-zoom
6. Navigate to bottom — verify "Anterior" / "Siguiente" chapter navigation
7. Go back to landing — verify "Continuar leyendo" appears with progress
8. Try search: type "petroleo" — verify results appear with highlighted matches
9. Open Chrome DevTools (F12) -> Device Toolbar (Ctrl+Shift+M) -> Select "Galaxy S5" (360px) — verify:
   - All content readable
   - Tables scroll horizontally
   - Cards stack single column
   - Touch targets are 44px+
10. Go offline (DevTools Network tab -> "Offline") -> Navigate between already-visited chapters -> verify they load
11. Share a URL with hash (e.g., /plan/fundamentos#reservas-petroleras) — verify it scrolls to the correct heading
  </action>
  <verify>Human visual and functional verification of all 11 checks above.</verify>
  <acceptance_criteria>
    - All 11 verification steps pass without issues
    - All 5 ROADMAP Phase 2 success criteria confirmed
  </acceptance_criteria>
  <done>Human approves the complete content migration experience. Phase 2 is ready for verification.</done>
</task>

</tasks>

<verification>
All 5 ROADMAP success criteria for Phase 2:
1. User can browse all 50+ plan documents within the El Plan tab, with previous/next navigation
2. User can search any term across all content and get results while offline
3. User can share a direct URL to any section and the recipient lands on exact content
4. Admonitions, Mermaid diagrams (static SVG), and wide tables render correctly on 360px
5. Every data point shows its source via tooltip or footnote (source badges)
</verification>

<success_criteria>
- Build completes without errors
- All tests pass
- Bundle within 200KB budget
- Human verification approves the reading experience
- All 5 ROADMAP Phase 2 success criteria met
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-migration/02-04-SUMMARY.md`
</output>
